{
  "_note": "P-Override: class determines the default extra step; one role value always overrides to a fixed process regardless of class. 3 classes, 4 roles. composition.override_value is passed to c4_override split. No output_category or functions — process selection is driven by PROCESS_MAP in the task spec.",

  "input_category": "entity.npc",
  "input_meta": {
    "name": [
      "name_pools.first",
      "name_pools.last"
    ],
    "repeat_name": [
      "name_pools.repeat_first",
      "name_pools.repeat_last"
    ],
    "properties": {
      "location": "locations"
    }
  },

  "inputs": [
    {
      "name": "class",
      "n_values": 3,
      "pool": "attributes.class",
      "nonce_pool": "attribute_slots.0",
      "nonce_label": "attribute_labels.0"
    },
    {
      "name": "role",
      "n_values": 4,
      "pool": "attributes.role",
      "nonce_pool": "attribute_slots.1",
      "nonce_label": "attribute_labels.1"
    }
  ],

  "composition": {
    "override_value": 3
  }
}
